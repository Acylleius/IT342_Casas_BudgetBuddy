spring.datasource.url=${DB_URL:jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres?sslmode=require}
spring.datasource.username=${DB_USERNAME:postgres.qyxrakgzfzhbfxjptfit}
spring.datasource.password=${DB_PASSWORD:}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.connection-timeout=3000

spring.web.resources.static-locations=classpath:/static/,file:../../web/

budgetbuddy.jwt.secret=${BUDGETBUDDY_JWT_SECRET:budgetbuddy-finals-local-secret-change-me}
budgetbuddy.jwt.access-token-minutes=${BUDGETBUDDY_ACCESS_TOKEN_MINUTES:60}
budgetbuddy.jwt.refresh-token-days=${BUDGETBUDDY_REFRESH_TOKEN_DAYS:14}

spring.mail.host=${SMTP_HOST:}
spring.mail.port=${SMTP_PORT:587}
spring.mail.username=${SMTP_USERNAME:}
spring.mail.password=${SMTP_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
